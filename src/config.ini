[PATHS]
# Diretório onde os dados brutos e processados serão salvos
data_dir = data

# Diretório onde o modelo TF-IDF será salvo
models_dir = models

# Diretório onde os relatórios gerados serão salvos
reports_dir = reports

[MODELS]
# Número máximo de features no vetor TF-IDF
tfidf_max_features = 5000

[TWITTER]
# Token de autenticação da API do Twitter (IGNORADO no modo offline)
bearer_token = SUA_CHAVE_AQUI
